# `planning/` — fork-only working documents

Everything under this directory is **fork-only** and must never appear in a pull request against
`cagnulein/qdomyos-zwift`.

Upstream PRs are cut as separate branches off `upstream/master` containing only the code change and
any `docs/` updates that genuinely belong upstream. If a planning branch is ever the base for a PR
branch, drop this directory from it first.

| Area | Docs |
|---|---|
| Concept2 PM5 (issues #4854, #4855, #4856, #4857) | [`pm5/`](pm5/) |
